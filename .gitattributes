# Normalize line endings to LF for text files; auto-detect binary.
* text=auto eol=lf

# Explicit binary assets (never normalize).
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
